node_modules/
.env

# Runtime data — db, uploads, sessions, temp files. Kept out of git so app
# updates (git pull/push) never touch live data. Use /admin/backup (exports a
# .zip of the link map + local files) for backups instead.
data/
*.sqlite
*.sqlite3
*.db
npm-debug.log*
.DS_Store
