services:
  app:
    build: .
    image: mindgrove-media-manager:latest
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    volumes:
      - data:/app/data

volumes:
  data:
